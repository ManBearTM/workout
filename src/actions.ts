import { createAction } from 'redux-actions'

export const addSchedules = createAction('ADD_SCHEDULES')
export const addScheduleSteps = createAction('ADD_SCHEDULESTEPS')
export const addWorkouts = createAction('ADD_WORKOUTS')
export const addDays = createAction('ADD_DAYS')